/**
 * This is a parse util which builds component set from component definition.
 * It may be used as a separate independent tool.
 * It can work on node environment and in a browser as well, so don't add any node dependencies like "fs" etc.
 */

export * from './parser-utils';
